// The master is declared in both stages - it grows the sprite up there and crossfades the
// mark here. The three character keys are decided beside the colour they read.
uniform float glyph, glyphTone, glyphHash, glyphRain;
