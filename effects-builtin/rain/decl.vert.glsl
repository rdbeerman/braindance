uniform float rain, rainSpeed, rainSpan;
// A second cell holding program time, so timeline-check --mutate rain-accumulates can
// integrate exactly this line rather than reddening the ripple and the raster with it.
uniform float rainPhase;
